apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metrics-app
  template:
    metadata:
      labels:
        app: metrics-app
    spec:
      containers:
        - name: metrics-app
          image: rmnev/metrics-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: metrics-app-config
---
apiVersion: v1
kind: Service
metadata:
  name: metrics-app
spec:
  selector:
    app: metrics-app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
